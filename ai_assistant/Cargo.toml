[package]
name = "ai_assistant"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
ron = "0.8"
bincode = "1.3"
clap = { version = "3.2", features = ["derive"] }
chrono = "0.4"
version_watcher = { path = "../version_watcher" } # Ajout de `version_watcher`
semver_planner = { path = "../semver_planner" }   # Ajout de `semver_planner`
