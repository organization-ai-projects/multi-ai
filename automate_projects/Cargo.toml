[package]
name = "automate_projects"
version = "0.1.0"
edition = "2024"

[dependencies]
ron = "0.8"
serde = { version = "1.0", features = ["derive"] }
clap = { version = "4.4", features = ["derive"] }
regex = "1.10"
similar = "2.3"
chrono = "0.4"
glob = "0.3"
