[package]
name = "publish_unl"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.11", features = ["multipart", "blocking"] }
clap = { version = "4.0", features = ["derive"] }
