// Exemple de “mutation” d’une stratégie
if rand::random::<f32>() < 0.1 {
    println!("Mutation d’une stratégie de parsing !");
    // Swap parser, randomise le selector, ajoute une nouvelle stratégie, etc.
    // Tu peux muter le Selector de Wiktionary par exemple, et voir si ça donne d’autres résultats.
}
